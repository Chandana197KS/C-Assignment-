#include <stdio.h>
#include <stdlib.h>
#include "Assignment1.h"
#include<assert.h>

int main()
{
 assert(sum_of_n_numbers_persent_in_the_file()==15);
generate_square_of_a_number();
reverse_order();
assert(number_of_times_substring_present("the")==1);
assert(length_of_longest_sentence()==52);
return 0;
}

